---
last_updated: 2026-05-13T01:10:00Z
status: Impeccable audit completed. Homepage + drawing package reviewed. Decision: merge drawing experience INTO homepage (option 2 then full option 1). Next session: plan the merge at xhigh effort using /impeccable shape then craft.
---

# Session Context: Spatial Portfolio

> **For AI agents:** Read `docs/the-plan-worth-doing.md` for the original vision and architecture.
> Read `PRODUCT.md` for brand voice, users, anti-references, and design principles.
> This file is your operational context: current state, coordinates, and what to do next.

---

## IMMEDIATE NEXT SESSION

**Goal:** Plan and execute merging the drawing package experience INTO the homepage.

**Activation sequence:**
1. Set effort to `xhigh`
2. Run `/impeccable` with target being the merge plan
3. Read `PRODUCT.md` and `docs/the-plan-worth-doing.md` first (context loader)
4. Use `shape` to plan the merge, then `craft` to implement

**The merge strategy (two phases):**

### Phase 1: Option 2 — Drawing as hero, homepage as spine
- Replace the homepage hero section with the title block station from the drawing package
- Replace the homepage project section with scroll-driven detail views (A-D stations)
- Services, references, and contact remain as traditional sections below the drawing experience
- The first ~60% of scroll IS the drawing; the bottom ~40% transitions back to a clean footer/contact
- The `/drawing-package` route redirects to `/#drawing` or becomes unnecessary

### Phase 2: Full Option 1 — Drawing IS the site
- Convert services into drawing annotations on the substrate (like General Notes on a real print)
- Convert references/testimonials into annotation callouts on the substrate
- Contact lives in the title block permanently
- The entire site is the scroll-driven blueprint experience
- No traditional portfolio sections remain

---

## IMPPECCABLE AUDIT FINDINGS (2026-05-13)

Full audit of both homepage and drawing package. These must be addressed during or before the merge.

### CUT (remove)
1. **Tech marquee / skill ticker** — Scrolling skill tags repeat 3x. Pure filler. PRODUCT.md: "Show the work, not the wrapper."
2. **"Most Popular" badge** on AI Integration service card — SaaS cliché. PRODUCT.md: "earned confidence, not performed confidence."
3. **Duplicate testimonials** — References section renders Kevin B, Lisa Fullem, Darrin Phipps 4x each (12 cards, 3 unique quotes). Bug. Keep 3 originals.
4. **"Media Frame" labels** on project cards — Dev debug label, not visitor-facing UI.

### FIX (broken)
5. **Hero subhead truncated** — "I design mechanical systems and build software and AI tools that make work" — sentence ends mid-thought. Tooltip "software as operational proof" doesn't complete it.
6. **"Review Media Strip [07]" buttons** — Reads like admin action, not visitor invitation.
7. **Drawing package page: duplicate h1** — Title block station renders `<h1>Mark Hintz</h1>` twice.
8. **About section repeats Darrin Phipps testimonial** — Same quote in About AND References.

### IMPROVE
9. **Service pricing cards feel SaaS** — $85/$100/$75 with bullet lists and CTAs is structurally identical to SaaS pricing. Needs to feel like a precision shop rate card.
10. **Project cards visually identical** — All 5 follow same template. PRODUCT.md anti-ref: "identical card grids." The drawing merge solves this.
11. **Engineering reel is visible placeholder** — Shows "STANDBY." Hide until content exists.
12. **Hero spec block STACK line is generic** — "SolidWorks · PDM · Python · AI Tooling" violates "specificity is the brand." ROLE and TOL lines are strong; STACK should match or get cut.
13. **Drawing package substrate is sparse** — Four stations on a large canvas. Add more drawing annotations, dimension lines, section markers for visual density.

### WHAT'S WORKING (preserve during merge)
- "Built from the shop floor up" headline
- About copy (technical, specific: "sin block, Micro Height and plug gages")
- DXF substrate + title block + revision history concept
- Project images show real work
- Contact section voice ("doesn't get laughed off the shop floor")
- Footer: "±0.0005" on the floor. TypeScript in the chair."

---

## Role & Operating Rules

- Act as a **Senior Creative Technologist and GSAP Architect**.
- Do not output generic vertical-scrolling web patterns.
- The viewport is pinned. The substrate moves, not the user.
- `Remotion` is **banned from UI work** — it renders offline MP4/WebM media only.
- All new spatial components must use `IntersectionObserver` for self-contained animation triggers.

---

## Milestone Log *(max 5 entries — drop oldest when adding new)*

1. **Impeccable audit complete** — Both pages reviewed. Merge decision made: drawing experience becomes the homepage. 13 findings (4 cut, 4 fix, 5 improve).
2. **Station T calibrated + responsive scaling** — Title Block reduced from 2400px→1000px width, fonts scaled 2x. Responsive `Math.min(1.2, vw*0.85/1000)` scale.
3. **Cinematic Hero & 3D Arrows** — Hero overlay on 3D substrate with movie-credits typography. ProjectZone CSS 3D leader lines from Z=0 floor to Z=400 floating detail circles.
4. **Stations C & D validated** — Both confirmed in-frame at 975x550, 1440x900, 390x844.
5. **GSAP Camera Engine + 3D Rig** — Scroll-pinned substrate, whip-pan + scale timeline, `perspective: 4000px` + `rotateX: 35°`.

---

## CURRENT STATE & COORDINATES (THE MAP)

### Architecture: Two Separate Routes (pre-merge)

**Route 1: `/` (Homepage)** — Standard vertical-scroll portfolio
- Hero, Engineering Reel, About, Projects (5 cards), Services, Tech Marquee, References, Contact/Footer
- Nav: About, Projects, Services, Case Studies, Drawing, Contact
- Components in `src/components/`

**Route 2: `/drawing-package` (Drawing Package)** — Scroll-driven blueprint experience
- DXF substrate with 4 detail stations (A-D), hero overlay, title block station (T)
- Standalone route: no navbar, no theme toggle
- Main component: `src/pages/DrawingPackagePage.tsx`

### Map Substrate (The 3D Rig)

- **Asset:** `Lower Receiver-Machined Forging (22).svg`
  - Filter: `invert(1)` → white linework; `mix-blend-screen` on `bg-slate-950`
  - DOF blur state must include `invert(1)` or lines turn black
- **Native Resolution:** 8800px (W) × 6800px (H)
- **CSS 3D Geometry:**
  - Outer container: `perspective: 4000px`, `perspectiveOrigin: 50% 40%`
  - Substrate div: `transformStyle: preserve-3d`
  - Max safe substrate Y at `scale: 1.2`: `4000 / (sin(35°) × 1.2) ≈ 5806px` ← never exceed

### Active Stations & Camera Stops

**Station A** [`id="A"`, TRIGGER GUARD RADIUS]
- Component Position: `left: 1450px, top: 3200px`
- Camera Stop: `x: -1400` on desktop/tablet, `x: -1400 + 0.55*(vw - 975)` under `768px`; `y: -3730 - max(0, 720 - vh)*0.35, scale: 1.2, rotateX: 0`

**Station B** [`id="B"`, BUFFER TUBE SOCKET]
- Component Position: `left: 5567px, top: 833px`
- Camera Stop: `x: -6320 + 0.495*(vw - 975), y: -740 + 0.47*(vh - 550), scale: 1.2, rotateX: 35`

**Station C** [`id="C"`, INDUSTRIAL DEWATERING PUMP]
- Component Position: `left: 3500px, top: 4200px`
- Camera Stop: `x: -3920 + 0.5*(vw - 975), y: -4980 - max(0, 720 - vh)*0.3, scale: 1.2, rotateX: 0`

**Station D** [`id="D"`, RENDERINGS]
- Component Position: `left: 6800px, top: 4000px`
- Camera Stop: `x: -7880 + 0.48*(vw - 975), y: -4740 - max(0, 720 - vh)*0.3, scale: 1.2, rotateX: 0`

**[Hero Overlay]** — Pre-scroll overlay. Camera starts zoomed out showing full substrate.
- Component: inline in `DrawingPackagePage.tsx` (heroRef, heroTextRef)
- Hero stop: `getHeroStop()` — fits substrate in viewport at 82% with margin
- z-index: 60 (above noise overlay at 50)

**Station T** [`id="T"`, TITLE BLOCK]
- Component: `TitleBlockStation.tsx` — ANSI title block with revision table, contact info, status
- Position: `left: 6400px, top: 5800px, width: 1000px`
- Camera Stop: `getTitleBlockStop()` — responsive scale, center (6900, 6025)

### GSAP Timeline Sequence

| Stop | x | y | scale | rotateX |
|---|---|---|---|---|
| Hero (start) | `getHeroStop()` | `getHeroStop()` | `fitScale*0.82` | 0° |
| Station A | `-1400` desktop, responsive | `-3730 - max(0, 720 - vh)*0.35` | 1.2 | 0° |
| Whip / transit | -4800 | -2000 | 1.6 | 0° |
| Tilt stop (Station B) | `-6320 + 0.495*(vw - 975)` | `-740 + 0.47*(vh - 550)` | 1.2 | 35° |
| Transit B→C | -5000 | -3800 | 1.4 | 10° |
| Station C | `-3920 + 0.5*(vw - 975)` | `-4980 - max(0, 720 - vh)*0.3` | 1.2 | 0° |
| Transit C→D | -6800 | -3600 | 1.5 | 5° |
| Station D | `-7880 + 0.48*(vw - 975)` | `-4740 - max(0, 720 - vh)*0.3` | 1.2 | 0° |
| Transit D→T | `vw/2 - 6900*1.1` | `vh/2 - 5800*1.1` | 1.1 | 5° |
| Title Block (Station T) | `vw/2 - 6900*scale` | `vh/2 - 6025*scale` | `min(1.2, vw*0.85/1000)` | 0° |

### Key Implementation Notes

- `useLayoutEffect` (not `useEffect`) — fires before paint, required for GSAP initial state
- `IntersectionObserver` threshold: `0.2`; each `ProjectZone` animates once per page load and unobserves
- DOF blur driven by GSAP scroll timeline, not IntersectionObserver
- Calibration mode at `?calibrate=1` — forces ProjectZone overlays visible, outlines station boxes
- `window.scrollTo()` needs overflow override to work in drawing-package route
- Scroll total: `end: '+=7000'`. Hero→A takes ~15%, Title Block ~15% at end
- Nav link "Drawing" in `portfolioData.navigation` — links to `/drawing-package`
- Hidden calibration mode: `?calibrate=1`

---

## Technical Debt & Known Issues

- `npm run build` needs re-run after any stop changes
- Full `npm run lint` fails on pre-existing Remotion/UI lint issues (unrelated)
- Full `npm run test` fails on image filename case mismatch: expected `.JPG`, received `.jpg` (unrelated)
- Testimonials duplicate rendering bug (audit finding #3)
